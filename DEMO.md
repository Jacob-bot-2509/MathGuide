# MathGuide 大创展示脚本

> 展示前逐条过一遍「环境准备」,现场按「演示动线」走;每步都给了预期与讲解话术,
> 翻车看「预案」。动线全程 3~5 分钟,全程在浏览器操作,无命令行露出。

## 一、环境准备(展示机,提前一天做完)

1. **部署**:`git clone` → 前端 `npm install`(含 KaTeX 资源,PDF 导出必需)→
   后端 `pip install -r requirements.txt`
2. **密钥**:把 `.env.local` 手动拷到 `backend/`(含 API key,**不入库**,
   展示机没有它,所有模型调用全挂)
3. **自检**:`cd backend && python tools/preflight.py` —— 全绿方可演示
4. **预热**:把动线第 5 步的研究问题预先问一遍(回答进缓存,现场秒回,
   不用等 60 秒检索)
5. **启动**:仓库根目录双击 `dev.bat`(后端 8000 热重载守护 + 前端 5173)

网络说明:LLM 与 embedding **强制直连**(不依赖系统代理);研究搜索五源中
StackExchange 等外网源在无代理时可能不通 —— 单源失败自动隔离,不报错,
现场开不开代理都能演示(开代理则五源最全)。

## 二、演示动线

### 1. 登录(约 15 秒)

- 操作:打开 http://127.0.0.1:5173 → 演示账号登录
- 预期:进入学习页,指令栏、知识板块、科技风界面
- 话术:「这是 MathGuide,面向高等数学学习的 AI 助手。界面是我们自己设计的前后端。」

### 2. 知识问答(约 40 秒)

- 操作:输入「什么是泰勒展开」,等回答;再问「What is Taylor expansion?」
- 预期:严谨定义 + 形象化理解 + 公式渲染(注意公式是**规范数学排版**,不是图片)
- 话术:「知识问答走检索增强(RAG):先检索知识库,再交给大模型综合。
  支持中英双语,公式用 KaTeX 按数学规范渲染。」

### 3. 闲聊与路由(约 20 秒)

- 操作:输入「你会什么」
- 预期:有温度的闲聊式回应,**不会**误判成知识直答
- 话术:「意图路由是系统设计重点:闲聊、知识、证明、研究四类问题
  自动分流到不同处理链路,各走各的模型角色。」

### 4. 证明题 + 双角色复核(约 60 秒,答辩加分点)

- 操作:输入「求证:当 x>0 时 e^x > 1+x」
- 预期:deep 强模型完整证明,文末附「🔍 复核」段(第二个模型独立检查)
- 话术:「证明类问题路由到 235B 强模型,解答完成后由**另一个模型独立复核**,
  低置信度会主动标注 —— 这是我们的答案质量保障机制。」

### 5. 研究深度搜索(约 60 秒,数据能力展示)

- 操作:输入「泰勒展开的最新研究进展」(预热过,秒回)
- 预期:综述 + **编号引用区**,来源标注 arXiv / zbMATH 等
- 话术:「研究型提问走深度搜索:同时检索五个学术源,按权威性精排,
  由大模型综合成带引用的综述 —— 数据、结论都可溯源。」

### 6. 导出 PDF(约 60 秒,成果可交付的展示点)

- 操作:点研究回答下方的「导出 PDF」→ 选「中文」→ 等待 → 自动下载
- 预期:一份排版正式的综述 PDF(封面/摘要/关键词/分节/参考文献,公式排版规范)
- 话术:「学习成果一键交付:大模型把综述重组为结构化文档,支持中英双语,
  公式与页面内渲染完全一致,直接下载 PDF 带走。」

## 三、翻车预案

| 现场状况 | 应对 |
|---|---|
| 网络挂了 / API 限流 | 知识直答与「框架三步走」降级模式仍可演示(不依赖 LLM 也答得出来) |
| 回答明显异常 | 等几秒重问一次(限流窗口 60 秒 6 问);仍异常则跳过该步,走下一步 |
| 语音识别失灵(嘈杂/权限) | 跳过语音环节,全程打字(动线未依赖语音) |
| PDF 导出超 60 秒 | 属正常范围,用「大模型重组+排版转码」话术拖时间 |
| 前端 5173 打不开 | 确认 dev.bat 窗口还开着;前端窗口关闭即服务停止,双击 dev.bat 重开 |

## 四、答辩常见追问(备答)

- **模型从哪来**:阿里云通义 qwen 系(main/deep/long)+ 智谱 glm(backup),
  四角色按需路由,异平台容灾
- **知识库怎么来的**:23 篇自建数学知识文档(markdown+LaTeX)→ 切片 →
  混合检索(关键词/二元组/向量)
- **怎么保证答案可信**:知识问题有来源可查;解答有双模型复核;
  研究结论有编号引用
- **成本**:用量记账 usage.json 可量化,演示期单次问答量级几分钱
