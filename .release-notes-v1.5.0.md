# MathGuide v1.5.0 —— 从"能答"到"好用":测试期就绪的大版本

v1.4.0 → v1.5.0,累计 11 个提交,围绕三个目标:交互像人、模型用足、测试期敢开放。

## 🎯 一、交互体验(用户看得见的每一处)

- **流式输出不抢滚**:生成中上拉阅读不再被拽回底部,贴底才自动跟随,滚回底部恢复跟随;
- **停止生成**:生成中发送键变黄色"停止生成",可中断长回答,已生成内容保留;
- **文件导入三通道**:本地选择 / 桌面拖拽(电脑端,虚线遮罩提示)/ 剪贴板粘贴导入(移动端,＋菜单"粘贴导入");文本与 PDF 自动提取正文交给模型阅读理解,扫描版 PDF 友好提示;
- **章节知识导航交互化**:上传教材(txt/md/PDF)→ 一键划分章节列表(胶囊按钮)→ 点章节出该章知识指引 → 末尾右对齐**黄色「返回」框**跳回列表;已讲解章节直接跳转不重复生成;教材按会话缓存在后端,点击只发章节名;
- **问题记录(归纳本)**:数学题解答完成后末尾弹出「是 / 否」按钮——点「是」把题目与解答(含参考思路)收纳成档并提示"加入成功,后续可以用于温故知新",随后问"还有需要帮你解答的吗?";点「否」形式性询问;一次性提问若干题统一收纳在一个档;点指令栏"📓 问题记录"输出题目+参考解答+参考思路,点击题目跳回原问答温故知新。原「错题归纳」更名而来,「例题精讲」板块删除(五大指令精简为四大);
- **语音念法速查贴士**:语音输入进行中麦克风旁弹出 📓 本子图标(纯图标),点开查询数学符号标准念法,内置**中英双语一键切换**;内容六组约 90 条:函数与符号(含任意字母函数命名 f/g/φ/ψ/u/v/大写 F/反函数)、希腊字母表 18 条、高等数学符号 22 条、更高阶符号 20 条(伽马函数/拉普拉斯/张量/正态分布…)、式子结构(含高阶导"f 两撇/n 阶导/d²y/dx²/混合偏导"读法)、完整示例;
- **语音念题 → 数学符号转写(三档提速,感知近零)**:照着贴士念题("x 的平方加 y 的平方等于 z 的平方"),点停麦克风瞬间本地规则速转先出符号(0ms),说话期间的停顿已触发后台预热转写,LLM 精修以 SSE 流式无感覆盖(根号整体、分数方向、复杂 LaTeX);转写失败回退原文不阻断输入,可修改后再发送;
- **板块升级**:新增"概率论与数理统计"归纳模块(关键词 60+,中英双语,知识库文档同步扩写),封闭集召回 55/55 = 100%。

## 🧠 二、模型层(把花的每一分钱用在刀刃上)

- **双平台四角色按需路由**:阿里云(qwen3.8-flash / qwen3-235b-a22b / qwen-long)+ 智谱(glm-5.2);证明/竞赛 → deep 强模型、教材/长文档 → long、研究综合 → deep、日常 → main(便宜快);main 故障自动切 backup(异平台容灾),任何角色未配置自动回退;
- **双角色验证智能体**:数学题解答完成后由另一角色复核,结论以引用小字标注在答案末尾(🔍 复核),闲聊不触发、失败静默跳过;
- **多轮对话升级**:历史 8→16 轮,更早对话每 8 轮滚动压缩成摘要附在 system 中——50 问的长辅导不丢早期上下文;
- **研究型提问全链路**:LLM 查询改写(口语化提问拆高质量检索词,失败回退规则版)→ 五源并发搜索 → LLM 精排 → deep 综合解答 + 后端拼装引用区(幻觉防线);搜索与综合**双层缓存**,重复问题秒回;
- **长文档能力**:教材/长文本上限 3 万字,发挥 QwenLong 长上下文;PDF 前端提取文本层直接喂入。

## 🔍 三、检索层

- **Embedding 语义混合检索上线**:text-embedding-v3 全库向量化(缓存落盘,重启不重复计费),"换一种说法"也能命中;关键词 + 向量混合打分;
- **闲聊噪声门限**:纯向量命中需相似度 ≥0.55,杜绝"你好呀"低相似度误命中数学文档(回归验证 55/55);
- **降级保护**:embedding 初始化/查询/重建任一环节失败自动退回关键词模式,检索链路永不因向量服务故障崩溃。

## 🛡 四、安全与运维(测试期敢开放 10~20 人的底气)

- **服务端输入上限**:prompt 统一截断 3.2 万字,前端限制之外的最后保险;
- **限流**:每用户 60 秒最多 6 问(超出 429 友好提示),问候不占额度;
- **token 24 小时过期**:验证时逐条检查,泄露不再无限期可用;
- **用量统计(电表)**:每次调用自动记账(谁/指令/字数/模型角色),`python tools/usage.py` 终端报表 + `/api/stats` 接口,测试期成本一目了然;
- **GBK 控制台崩溃修复**:模型输出含下标 ₀ 等字符曾打断回答链路,启动强制 UTF-8 + 行缓冲;
- **Mock 全量剔除**:前端 mock/ 目录与 USE_MOCK 分支全部删除,登录与聊天仅走真实后端。

## 📚 五、文档与工程

- 前后端 README 全面重写(四角色路由、embedding 配置、安全底座、接口契约、用量统计、工具清单);
- 质检体系:召回 55/55、路由 50 用例、深度搜索 10 用例、演示前自检 preflight 全绿。

## 🗺 下一阶段

① 测试期部署(云服务器,10~20 人实测);② 科学研究板块前端(深度搜索后端已就绪);③ 语音识别切国内服务(讯飞/百度,手机端可用);④ 视觉识图(拍题解答);⑤ 验证智能体采样策略(成本旋钮)。

---

**测试账号**:13600136000 / demo123456(验证码 123456)
**运行**:仓库根目录 `dev.bat` 一键启动,浏览器 http://localhost:5173
